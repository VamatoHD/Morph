[package]
name = "vamatohd/morph"
description = "TweenService wrapper"
license = "MIT"
authors = ["Vamato"]

version = "1.0.0"

registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

exclude = ["**"]
include = ["src", "src/*", "default.project.json", "wally.lock", "wally.toml"]
